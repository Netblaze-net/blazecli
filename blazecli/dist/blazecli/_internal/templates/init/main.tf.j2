terraform {
  backend "local" {

  }
}

provider "google" {
  region = "{{ region }}"
  zone   = "{{ region }}-a"
}

resource "google_storage_bucket" "state" {
  name          = "{{ backend_bucket }}"
  location      = "US"
  force_destroy = false
}
